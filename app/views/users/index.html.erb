<main data-view-id="12" class="main">
  <div class="header">
    <div class="header__content">
      <div class="header__heading">
        <p class="header__subtitle">GESTIÓN DE USUARIOS</p>
        <h1 class="header__title">LISTA DE ADMINISTRADORES</h1>
      </div>
      
      <div class="header__actions">
        <div class="header__actions header__actions--tablet">
          <%= link_to new_user_registration_path(:role => "admin"), class: "button-primary" do %>
            <%= image_tag('add-icon.svg', alt: "icono agregar administrador", class: "sidebar__icon") %>
            <span class="ms-1">Agregar administrador</span>
          <% end %>
        </div>
      
        <%= render 'shared/sidebar_button' %>
      </div>
    </div>

    <div class="header__actions header__actions--phone">
      <%= link_to new_user_registration_path(:role => "admin"), class: "button-primary" do %>
        <%= image_tag('add-icon.svg', alt: "icono agregar administrador", class: "sidebar__icon") %>
        <span class="ms-1">Agregar administrador</span>
      <% end %>
    </div>
  </div>

  <%= render 'user_filters', resource: @admin_q, resource_path: users_path, data_name: "admins" %>

  <section class="card-list mb-4">
    <div class="d-flex justify-content-between align-items-center" data-controller="filters">
      <h4 class="card-list__title">
        <%= image_tag('card-list-icon.svg', alt: "icon lista de proyectos") %>
        ADMINISTRADORES
      </h4>
      <a class="text-danger" id="clear-filters-button" style="display: none; text-decoration: none;" data-action="click->filters#clearFilters">Limpiar filtros &times;</a>
    </div>

    <div id="users" class="card-list__items">
      <% @users.each do |user| %>
        <%= render user %>
      <% end %>

      <% if !@admin_q.result.any? %>
        <p class="text-center text-black-50 fst-italic my-4"><i class="bi bi-info-circle me-2"></i>Lista de administradores vacía</p>
      <% end %>
    </div>

    <% if !@users.empty? %>
      <div class="card-list__pagination">
        <%= render "shared/pagination_count", object: @users %>
        <%= will_paginate @users, inner_window: 1 %>
      </div>
    <% end %>
  </section>
</main>